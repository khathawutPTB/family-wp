VITE_API_URL=http://localhost:4000/api

# Public half of the backend's VAPID key pair (safe to expose — only the
# private half, kept server-side, is secret). Must match VAPID_PUBLIC_KEY
# in the backend's .env.
VITE_VAPID_PUBLIC_KEY=
